def num_to_word(num):
    words = {
        0: 'ноль',
        1: 'один',
        2: 'два',
        3: 'три',
        4: 'четыре',
        5: 'пять',
        6: 'шесть',
        7: 'семь',
        8: 'восемь',
        9: 'девять'
    }
    return words.get(num)


Вывод:
print(num_to_word(3)) 3*любое число от 0 до 9

# Файл с задачай нужно перевести в расширение .py
# Решение записано в виде функции. Это следующее задание. Его надо представить без функции. Нужно выполнить в отдельном файле
# Решение нужно закуить через git с локального устройства!
